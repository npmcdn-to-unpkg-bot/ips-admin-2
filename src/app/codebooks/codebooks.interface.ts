export interface ICodebooks {

    codeBookId: number;
    codeBookName: string;
    codeWords: string;
    codeWordCount: number;
    symbolPattern: string;
    //lastUpdateDate: ?
    lastUpdateBy: string;
}